node_modules
.git
.gitignore
.env
.env.local
.DS_Store

# 排除原始后端代码（只打包混淆后的代码）
server/*
server.js
!dist-server

# Do not copy plugins into the image - they will be mounted via volumes
# Plugins are loaded dynamically at runtime
plugins/*
data/*
logs/*
tmp/*
tmp_packages/*
# Build artifacts
# dist  ← 已注释，需要复制本地构建的dist到镜像
*.log
.vscode
.idea
Navlink-plugins
navmanage
website
deploy
chrome-extension
dist-plugins
docs
scripts
videox