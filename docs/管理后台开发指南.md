# NavLink 管理后台开发指南

> 版本：1.0.0  
> 更新时间：2025-12-06

##目录

- [1. 概述](#1-概述)
- [2. 架构设计](#2-架构设计)
- [3. 认证与权限](#3-认证与权限)
- [4. 核心页面](#4-核心页面)
- [5. 表单开发](#5-表单开发)
- [6. 数据可视化](#6-数据可视化)
- [7. API调用](#7-api调用)
- [8. 开发实践](#8-开发实践)

---

## 1. 概述

NavLink 管理后台提供全面的站点配置和管理功能。

**核心功能模块**：

📊 **仪表板** - 系统概览、统计信息  
📝 **内容管理** - 基础设置、导航、分类、推荐等  
🔌 **插件管理** - 插件市场、安装、配置 
👥 **用户管理** - 用户、权限、租户  
⚙️ **系统设置** - AI配置、健康检查、数据管理、日志监控  

---

## 2. 架构设计

### 2.1 目录结构

```
src/apps/admin/
├── App.tsx                    # 路由配置
├── layout/
│   ├── AdminLayout.tsx        # 管理后台布局
│   ├── AdminNavbar.tsx        # 顶部导航
│   └── AdminSidebar.tsx       # 侧边菜单
├── pages/
│   ├── Auth/
│   │   └── Login.tsx          # 登录页
│   ├── Dashboard.tsx          # 仪表板
│   ├── Content/               # 内容管理
│   │   ├── BasicSettings.tsx  # 基础设置
│   │   ├── TopNavSettings.tsx # 导航设置
│   │   ├── HeroSettings.tsx   # 主视图设置
│   │   ├── PromoSettings.tsx  # 推荐设置
│   │   ├── CategorySettings.tsx # 分类设置
│   │   └── SidebarSettings.tsx # 侧边栏设置
│   ├── System/                # 系统管理
│   │   ├── Plugins/
│   │   │   ├── PluginList.tsx
│   │   │   └── PluginMarket.tsx
│   │   ├── Users.tsx
│   │   ├── Permissions.tsx
│   │   ├── AIConfig.tsx
│   │   ├── LinkHealth.tsx
│   │   ├── MediaManagement.tsx
│   │   ├── DataManagement.tsx
│   │   ├── Logs.tsx
│   │   └── Monitor.tsx
│   └── Plugins/
│       └── PluginList.tsx
├── components/
│   ├── charts/                # 图表组件
│   └── forms/                 # 表单组件
└── types/
    └── admin.ts               # 类型定义
```

### 2.2 路由配置

```typescript
// App.tsx
import { Routes, Route } from 'react-router-dom';
import AdminLayout from './layout/AdminLayout';
import Login from './pages/Auth/Login';
import Dashboard from './pages/Dashboard';
import { ProtectedRoute } from './components/ProtectedRoute';
import { adminRoutes } from './config/routes';

export default function AdminApp() {
    return (
        <Routes>
            <Route path="/login" element={<Login />} />
            
            <Route path="/*" element={
                <AdminLayout>
                    <Routes>
                        <Route path="/" element={<Dashboard />} />
                        {adminRoutes.map((route) => (
                            <Route
                                key={route.path}
                                path={route.path}
                                element={
                                    <ProtectedRoute 
                                        permission={route.permission}
                                        title={route.title}
                                    >
                                        <route.component />
                                    </ProtectedRoute>
                                }
                            />
                        ))}
                    </Routes>
                </AdminLayout>
            } />
        </Routes>
    );
}
```

### 2.3 AdminLayout 组件

```typescript
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useConfig } from '@/shared/context/ConfigContext';
import AdminNavbar from './AdminNavbar';
import AdminSidebar from './AdminSidebar';

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  const { isAuthenticated } = useConfig();
  const navigate = useNavigate();
  const [sidebarOpen, setSidebarOpen] = useState(true);

  // 未认证跳转登录
  React.useEffect(() => {
    if (!isAuthenticated) {
      navigate('/admin/login');
    }
  }, [isAuthenticated]);

  return (
    <div className="min-h-screen bg-gray-100">
      <AdminNavbar onToggleSidebar={() => setSidebarOpen(!sidebarOpen)} />
      
      <div className="flex">
        <AdminSidebar isOpen={sidebarOpen} />
        
        <main className={`flex-1 p-6 transition-all ${sidebarOpen ? 'ml-64' : 'ml-0'}`}>
          {children}
        </main>
      </div>
    </div>
  );
}
```

---

## 3. 认证与权限

### 3.1 登录流程

```typescript
// pages/Auth/Login.tsx
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useConfig } from '@/shared/context/ConfigContext';

export default function Login() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const { login } = useConfig();
  const navigate = useNavigate();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError('');

    try {
      await login(username, password);
      navigate('/admin/dashboard');
    } catch (err) {
      setError('用户名或密码错误');
    }
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <div className="max-w-md w-full bg-white rounded-lg shadow-md p-8">
        <h2 className="text-2xl font-bold text-center mb-6">管理后台登录</h2>
        
        {error && (
          <div className="mb-4 p-3 bg-red-100 border border-red-400 text-red-700 rounded">
            {error}
          </div>
        )}
        
        <form onSubmit={handleSubmit}>
          <div className="mb-4">
            <label className="block text-gray-700 mb-2">用户名</label>
            <input
              type="text"
              value={username}
              onChange={(e) => setUsername(e.target.value)}
              className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          
          <div className="mb-6">
            <label className="block text-gray-700 mb-2">密码</label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          
          <button
            type="submit"
            className="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 transition"
          >
            登录
          </button>
        </form>
      </div>
    </div>
  );
}
```

### 3.2 路由守卫

```typescript
// components/ProtectedRoute.tsx
import { Navigate } from 'react-router-dom';
import { usePermissions } from '@/shared/hooks/usePermissions';
import PermissionDenied from '../pages/PermissionDenied';

interface ProtectedRouteProps {
    children: React.ReactNode;
    permission?: string;
    title?: string;
}

export const ProtectedRoute: React.FC<ProtectedRouteProps> = ({ 
    children, 
    permission,
    title 
}) => {
    const { hasPermission, loading, isAdmin } = usePermissions();

    if (loading) {
        return <div className="p-6">Loading...</div>;
    }

    // 如果未配置权限或拥有指定权限或由于是管理员，则放行
    if (!permission || hasPermission(permission) || isAdmin()) {
        return <>{children}</>;
    }

    return <PermissionDenied />;
};
```

### 3.3 权限控制 (新增)

NavLink 采用集中式路由权限配置：

**配置文件：`src/apps/admin/config/routes.ts`**

```typescript
import { AdminRoute } from '../types';

export const adminRoutes: AdminRoute[] = [
    {
        path: '/dashboard',
        component: Dashboard,
        title: '仪表盘'
    },
    {
        path: '/users',
        component: Users,
        permission: 'user:view', // 需要 user:view 权限
        title: '用户管理'
    },
    // ...
];
```

**权限检查 Hook：`usePermissions`**

```typescript
const { hasPermission, isAdmin } = usePermissions();

if (hasPermission('system:edit')) {
    // 显示编辑按钮
}
```
```

---

## 4. 核心页面

### 4.1 仪表板 (Dashboard)

展示系统概览和关键指标。

```typescript
// pages/Dashboard.tsx
export default function Dashboard() {
  const [stats, setStats] = useState({
    totalLinks: 0,
    totalCategories: 0,
    totalPlugins: 0,
    activePlugins: 0
  });

  useEffect(() => {
    fetch('/api/stats')
      .then(res => res.json())
      .then(data => setStats(data));
  }, []);

  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">仪表板</h1>
      
      {/* 统计卡片 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <StatCard title="链接总数" value={stats.totalLinks} icon="fa-link" color="blue" />
        <StatCard title="分类数量" value={stats.totalCategories} icon="fa-folder" color="green" />
        <StatCard title="插件总数" value={stats.totalPlugins} icon="fa-plug" color="purple" />
        <StatCard title="运行中" value={stats.activePlugins} icon="fa-check-circle" color="orange" />
      </div>
      
      {/* 图表区域 */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <LinkHealthChart />
        <PluginStatusChart />
      </div>
    </div>
  );
}

function StatCard({ title, value, icon, color }) {
  const colors = {
    blue: 'bg-blue-500',
    green: 'bg-green-500',
    purple: 'bg-purple-500',
    orange: 'bg-orange-500'
  };

  return (
    <div className="bg-white rounded-lg shadow p-6">
      <div className="flex items-center justify-between">
        <div>
          <p className="text-gray-500 text-sm">{title}</p>
          <p className="text-3xl font-bold mt-1">{value}</p>
        </div>
        <div className={`w-12 h-12 ${colors[color]} rounded-lg flex items-center justify-center`}>
          <i className={`fas ${icon} text-white text-xl`}></i>
        </div>
      </div>
    </div>
  );
}
```

### 4.2 基础设置 (BasicSettings)

```typescript
// pages/Content/BasicSettings.tsx
export default function BasicSettings() {
  const { config } = useConfig();
  const [formData, setFormData] = useState({
    logoUrl: '',
    headerQuote: '',
    backgroundImage: ''
  });

  useEffect(() => {
    setFormData({
      logoUrl: config.logoUrl,
      headerQuote: config.headerQuote,
      backgroundImage: config.backgroundImage || ''
    });
  }, [config]);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    
    try {
      const response = await fetch('/api/config', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          ...config,
          ...formData
        })
      });

      if (response.ok) {
        alert('保存成功');
      }
    } catch (error) {
      alert('保存失败');
    }
  };

  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">基础设置</h1>
      
      <form onSubmit={handleSubmit} className="bg-white rounded-lg shadow p-6">
        <div className="mb-4">
          <label className="block text-gray-700 font-medium mb-2">Logo URL</label>
          <input
            type="text"
            value={formData.logoUrl}
            onChange={(e) => setFormData({ ...formData, logoUrl: e.target.value })}
            className="w-full px-4 py-2 border rounded-lg"
          />
        </div>

        <div className="mb-4">
          <label className="block text-gray-700 font-medium mb-2">顶部标语</label>
          <input
            type="text"
            value={formData.headerQuote}
            onChange={(e) => setFormData({ ...formData, headerQuote: e.target.value })}
            className="w-full px-4 py-2 border rounded-lg"
          />
        </div>

        <div className="mb-6">
          <label className="block text-gray-700 font-medium mb-2">背景图片 URL</label>
          <input
            type="text"
            value={formData.backgroundImage}
            onChange={(e) => setFormData({ ...formData, backgroundImage: e.target.value })}
            className="w-full px-4 py-2 border rounded-lg"
          />
        </div>

        <button
          type="submit"
          className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
        >
          保存设置
        </button>
      </form>
    </div>
  );
}
```

### 4.3 分类管理 (CategorySettings)

```typescript
// pages/Content/CategorySettings.tsx
export default function CategorySettings() {
  const { config } = useConfig();
  const [categories, setCategories] = useState<Category[]>([]);

  useEffect(() => {
    setCategories(config.categories || []);
  }, [config]);

  const handleAddCategory = () => {
    const newCategory: Category = {
      id: `cat-${Date.now()}`,
      name: '新分类',
      icon: 'fa-folder',
      items: []
    };
    setCategories([...categories, newCategory]);
  };

  const handleDeleteCategory = (id: string) => {
    setCategories(categories.filter(cat => cat.id !== id));
  };

  const handleSave = async () => {
    await fetch('/api/config', {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        ...config,
        categories
      })
    });
    alert('保存成功');
  };

  return (
    <div>
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-2xl font-bold">分类管理</h1>
        <button
          onClick={handleAddCategory}
          className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
        >
          <i className="fas fa-plus mr-2"></i>
          添加分类
        </button>
      </div>

      <div className="space-y-4">
        {categories.map((category, index) => (
          <CategoryEditor
            key={category.id}
            category={category}
            onUpdate={(updated) => {
              const newCategories = [...categories];
              newCategories[index] = updated;
              setCategories(newCategories);
            }}
            onDelete={() => handleDeleteCategory(category.id)}
          />
        ))}
      </div>

      <div className="mt-6">
        <button
          onClick={handleSave}
          className="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700"
        >
          保存全部更改
        </button>
      </div>
    </div>
  );
}

function CategoryEditor({ category, onUpdate, onDelete }) {
  return (
    <div className="bg-white rounded-lg shadow p-6">
      <div className="flex items-center gap-4 mb-4">
        <input
          type="text"
          value={category.name}
          onChange={(e) => onUpdate({ ...category, name: e.target.value })}
          className="flex-1 px-4 py-2 border rounded-lg"
          placeholder="分类名称"
        />
        <input
          type="text"
          value={category.icon}
          onChange={(e) => onUpdate({ ...category, icon: e.target.value })}
          className="w-40 px-4 py-2 border rounded-lg"
          placeholder="图标类名"
        />
        <button
          onClick={onDelete}
          className="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"
        >
          删除
        </button>
      </div>

      {/* 链接列表 */}
      <LinkListEditor
        items={category.items || []}
        onChange={(items) => onUpdate({ ...category, items })}
      />
    </div>
  );
}
```

---

## 5. 表单开发

### 5.1 表单组件模式

```typescript
// 受控表单组件
function FormField({ label, value, onChange, type = 'text', ...props }) {
  return (
    <div className="mb-4">
      <label className="block text-gray-700 font-medium mb-2">{label}</label>
      <input
        type={type}
        value={value}
        onChange={(e) => onChange(e.target.value)}
        className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
        {...props}
      />
    </div>
  );
}

// 使用
<FormField
  label="标题"
  value={title}
  onChange={setTitle}
  placeholder="请输入标题"
/>
```

### 5.2 表单验证

```typescript
function validateForm(data) {
  const errors = {};

  if (!data.name || data.name.trim() === '') {
    errors.name = '名称不能为空';
  }

  if (!data.url || !/^https?:\/\//.test(data.url)) {
    errors.url = '请输入有效的 URL';
  }

  return {
    isValid: Object.keys(errors).length === 0,
    errors
  };
}

// 使用
const handleSubmit = (e) => {
  e.preventDefault();
  
  const { isValid, errors } = validateForm(formData);
  
  if (!isValid) {
    setErrors(errors);
    return;
  }

  // 提交表单
  submitData(formData);
};
```

### 5.3 拖拽排序

```typescript
import { DndContext, closestCenter, KeyboardSensor, PointerSensor, useSensor, useSensors } from '@dnd-kit/core';
import { arrayMove, SortableContext, sortableKeyboardCoordinates, verticalListSortingStrategy } from '@dnd-kit/sortable';

function SortableList({ items, onReorder }) {
  const sensors = useSensors(
    useSensor(PointerSensor),
    useSensor(KeyboardSensor, {
      coordinateGetter: sortableKeyboardCoordinates,
    })
  );

  const handleDragEnd = (event) => {
    const { active, over } = event;
    
    if (active.id !== over.id) {
      const oldIndex = items.findIndex(item => item.id === active.id);
      const newIndex = items.findIndex(item => item.id === over.id);
      
      onReorder(arrayMove(items, oldIndex, newIndex));
    }
  };

  return (
    <DndContext
      sensors={sensors}
      collisionDetection={closestCenter}
      onDragEnd={handleDragEnd}
    >
      <SortableContext items={items} strategy={verticalListSortingStrategy}>
        {items.map(item => (
          <SortableItem key={item.id} id={item.id} item={item} />
        ))}
      </SortableContext>
    </DndContext>
  );
}
```

---

## 6. 数据可视化

### 6.1 使用 Recharts

```typescript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';

function LinkHealthChart() {
  const [data, setData] = useState([]);

  useEffect(() => {
    fetch('/api/stats/health-history')
      .then(res => res.json())
      .then(data => setData(data));
  }, []);

  return (
    <div className="bg-white rounded-lg shadow p-6">
      <h3 className="text-lg font-bold mb-4">链接健康趋势</h3>
      
      <LineChart width={500} height={300} data={data}>
        <CartesianGrid strokeDasharray="3 3" />
        <XAxis dataKey="date" />
        <YAxis />
        <Tooltip />
        <Legend />
        <Line type="monotone" dataKey="healthy" stroke="#10b981" name="健康" />
        <Line type="monotone" dataKey="unhealthy" stroke="#ef4444" name="异常" />
      </LineChart>
    </div>
  );
}
```

### 6.2 统计卡片

```typescript
function StatCard({ title, value, change, icon, color }) {
  return (
    <div className="bg-white rounded-lg shadow p-6">
      <div className="flex items-center justify-between mb-2">
        <span className="text-gray-500 text-sm">{title}</span>
        <div className={`w-10 h-10 bg-${color}-100 rounded-lg flex items-center justify-center`}>
          <i className={`fas ${icon} text-${color}-600`}></i>
        </div>
      </div>
      
      <div className="flex items-end justify-between">
        <span className="text-3xl font-bold">{value}</span>
        {change && (
          <span className={`text-sm ${change > 0 ? 'text-green-600' : 'text-red-600'}`}>
            {change > 0 ? '↑' : '↓'} {Math.abs(change)}%
          </span>
        )}
      </div>
    </div>
  );
}
```

---

## 7. API调用

### 7.1 RESTful API

```typescript
// 获取配置
const getConfig = async () => {
  const response = await fetch('/api/config');
  return response.json();
};

// 更新配置
const updateConfig = async (config: SiteConfig) => {
  const response = await fetch('/api/config', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(config)
  });
  return response.json();
};

// 删除分类
const deleteCategory = async (categoryId: string) => {
  const response = await fetch(`/api/categories/${categoryId}`, {
    method: 'DELETE'
  });
  return response.json();
};
```

### 7.2 错误处理

```typescript
async function apiCall(url: string, options?: RequestInit) {
  try {
    const response = await fetch(url, options);
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    return await response.json();
  } catch (error) {
    console.error('API Error:', error);
    throw error;
  }
}

// 使用
try {
  const data = await apiCall('/api/config');
  setConfig(data);
} catch (error) {
  setError('加载失败');
}
```

---

## 8. 开发实践

### 8.1 代码组织

✅ 按功能模块组织页面  
✅ 提取公共组件到 components/  
✅ 使用 TypeScript 类型定义  
✅ 统一的错误处理和加载状态  
✅ 使用 `tsconfig.json` 配置路径别名 (`@/`)  

### 8.2 样式规范

使用 Tailwind CSS：

```typescript
// 卡片
className="bg-white rounded-lg shadow p-6"

// 按钮 - 主要
className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"

// 按钮 - 危险
className="px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"

// 输入框
className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"

// 标题
className="text-2xl font-bold text-gray-800"
```

### 8.3 性能优化

```typescript
// 使用 React.memo 优化列表渲染
const CategoryItem = React.memo(({ category, onUpdate }) => {
  return (
    <div>
      {/* 组件内容 */}
    </div>
  );
});

// 使用 useMemo 缓存计算结果
const filteredCategories = useMemo(() => {
  return categories.filter(cat => 
    cat.name.toLowerCase().includes(searchQuery.toLowerCase())
  );
}, [categories, searchQuery]);
```

---

## 总结

本指南涵盖了管理后台开发的核心内容：

✅ 架构设计和路由配置  
✅ 认证流程和权限控制  
✅ 核心页面开发模式  
✅ 表单处理和数据可视化  
✅ API调用和错误处理  

继续学习：
- [后端API开发指南](./后端API开发指南.md)
- [共享模块开发指南](./共享模块开发指南.md)
- [主站前端开发指南](./主站前端开发指南.md)

---

祝开发愉快！🚀
